name: Battle E2E Diagnostic Once (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "obsolete one-shot workflow; safe to remove before merge"
